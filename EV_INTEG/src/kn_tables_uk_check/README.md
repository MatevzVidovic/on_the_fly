# Retired unique-key discovery tool

This dynamic metadata-discovery report is retired. Use the catalog-driven
manual SQL generator instead:

```sh
.venv/bin/python src/unique_constraint_sql/generate.py ev_pe_parc_h
```
